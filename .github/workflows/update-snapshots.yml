name: Update Snapshot Tests

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update_outputs:

    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11"]

    steps:
      - name: Check if GitHub App is installed
        run: |
          # Check installation
          if gh api /repos/${{ github.repository }}/installation --jq '.app_slug' | grep -q "quant-ci-bot"; then
            echo "✅ GitHub App quant-ci-bot is installed"
          else
            echo "❌ quant-ci-bot is NOT installed"
            echo "This repository requires the quant-ci-bot GitHub App to be installed."
            exit 1
          fi

      - name: Generate GitHub App token
        id: app-token
        uses: actions/create-github-app-token@v2
        with:
          app-id: ${{ secrets.QUANT_CI_BOT_APP_ID }}
          private-key: ${{ secrets.QUANT_CI_BOT_APP_PRIVATE_KEY }}

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}
          token: ${{ steps.app-token.outputs.token }}
          persist-credentials: false
          fetch-depth: 0

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v3
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install Poetry
        uses: snok/install-poetry@v1
        with:
          virtualenvs-create: true
          virtualenvs-in-project: true
          virtualenvs-path: .venv
          installer-parallel: true

      - name: Install dependencies
        run: |
          poetry install --no-root
          source .venv/bin/activate

      - name: Run snapshot tests to update expected outputs
        run: |
          echo "🧪 Running snapshot tests..."
          poetry run inv run-snapshot-test -t update
          echo "✅ Snapshot tests completed."

      - name: Commit updated snapshot outputs
        run: |
          echo "📦 Checking for changes in expected outputs of snapshot tests..."
          
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -u
          
          if git diff --cached --quiet; then
            echo "✅ No changes detected. Nothing to commit."
          else
            echo "📝 Changes detected — committing updated snapshot outputs..."
            git remote set-url origin https://x-access-token:${{ steps.app-token.outputs.token }}@github.com/${{ github.repository }}.git
            git commit -m "Update snapshot outputs"
            git push origin HEAD:${{ github.ref_name }}
            echo "🚀 Changes committed and pushed to '${{ github.ref_name }}'."
          fi
